class CommentController {
    function createComment(task_id, text, author) {
    // Create a new comment with the given text and author, and add it to the task with the given ID
    }

    function getComment(comment_id) {
    // Get the comment with the given ID
    }

    function updateComment(comment_id, text) {
    // Update the text of the comment with the given ID
    }

    function deleteComment(comment_id) {
    // Delete the comment with the given ID
    }
}